program Calculadora;

procedure MostrarMenu;
begin
  writeln('1. Sumar');
  writeln('2. Restar');
  writeln('3. Multiplicar');
  writeln('4. Dividir');
end;

function Operar(a, b, opcion: integer): real;
begin
  case opcion of
    1: Operar := a + b;
    2: Operar := a - b;
    3: Operar := a * b;
    4: if b <> 0 then
         Operar := a / b
       else
       begin
         writeln('Error: División por cero');
         Operar := 0;
       end;
  else
    writeln('Opción inválida');
    Operar := 0;
  end;
end;

var
  x, y, op: integer;
begin
  x := 10;
  y := 5;
  MostrarMenu;
  op := 3;  // Multiplicación
  writeln('Resultado: ', Operar(x, y, op):0:2);
end.