program Factorial;
var
  i, n: Integer;
  fact: LongInt;
begin
  write('Ingrese un número: ');
  readln(n);
  fact := 1;
  for i := 1 to n do
    fact := fact * i;
  writeln('El factorial es: ', fact);
end.
